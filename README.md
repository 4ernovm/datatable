Datatable
======

### Installation

Add `4ernovm/datatable` as a requirement to composer.json:

```json
{
    "require": {
        "4ernovm/datatable": "0.*"
    }
}
```

And then run `composer update`
